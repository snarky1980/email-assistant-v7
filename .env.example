###############################################
#  Email Assistant Environment Configuration  #
###############################################
# Copy this file to .env and fill in values.  #
# .env is ignored by git (see .gitignore).    #
###############################################

# REQUIRED: Your OpenAI API key
OPENAI_API_KEY=sk-REPLACE_WITH_YOUR_KEY

# OPTIONAL: Server listen port/host
PORT=3000
# HOST=0.0.0.0

# OPTIONAL: Logging & behavior flags
# LOG_REQUESTS=1   # Log each HTTP request
# HEARTBEAT=1      # 1 (default) logs a heartbeat every 60s; set 0 to disable
# SELF_PING=0      # Set 1 to periodically self-ping /api/ping (keeps free hosts awake)
# LOG_FILE=server.log

# Notes:
# - Do NOT expose OPENAI_API_KEY to client code; only the server uses it.
# - After editing .env, restart the server.
# - If running on a hosting platform, configure these as environment variables there.

###############################################
#  Admin authentication tokens                 #
###############################################
# The admin UI (/admin) and all /api/admin/* endpoints
# require a bearer token. Set at least one of these.
# Example (generate with `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`)
# Never commit real values to git.

# Primary token (required for admin features to be enabled)
ADMIN_TOKEN=

# Optional secondary token for rotation. If set, either token works.
# 1. Add ADMIN_TOKEN_2 and restart
# 2. Distribute new token, clients switch
# 3. Remove old ADMIN_TOKEN (move new into ADMIN_TOKEN) and unset ADMIN_TOKEN_2
# 4. Restart again
# ADMIN_TOKEN_2=


